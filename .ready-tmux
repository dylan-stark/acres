#!/usr/bin/env bash

tmux rename-session acres
tmux rename-window code
tmux new-window -n watch
tmux new-window -n scratch
tmux select-window -t acres:1

tmux send-keys -t acres:1 "nvim ." Enter
tmux send-keys -t acres:2 "make watch" Enter

